# Local SQLite database. Paths are resolved from the repository root.
DATABASE_URL="file:./data/assetflow.db"

# Used to choose the seeded organization when a new Employee signs up.
DEFAULT_ORGANIZATION_SLUG="northstar-works"

# Set to "true" only when HTTPS is used locally.
SESSION_COOKIE_SECURE="false"
